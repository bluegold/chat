# AGENTS.md

- まず既存コード・テスト・READMEを読み、推測で修正しない。
- 変更前に `git status` を確認し、ユーザーの未コミット変更を壊さない。
- 依頼範囲外の変更、無関係な整形、勝手なリファクタリングをしない。
- 既存の設計、命名、ディレクトリ構成、フレームワーク慣習を優先する。
- 大きな変更では短い方針を立て、小さな修正では最小差分で進める。
- バグ修正では可能なら再発防止テストを追加する。
- 変更後は関連テスト・lint・型検査・ビルドを可能な範囲で実行する。
- 検証できない場合やコマンドが失敗した場合は、理由と結果を報告する。
- 秘密情報、鍵、トークン、Cookie、証明書秘密鍵を表示・保存・コミットしない。
- 認証、認可、DB migration、公開API、依存関係更新は本番影響を明示する。
- 新しい依存関係は必要性が明確な場合だけ追加する。
- コメントは「何を」より「なぜ」を書く。
- 勝手に commit、reset、rebase、push しない。
- 完了報告は、変更内容・検証結果・未検証事項・残リスクを簡潔に書く。
